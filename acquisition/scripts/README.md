# Acquisition Scripts

Acquisition helper scripts will live here.

The initial goal is to port the existing `simple_acq` launch scripts, then consolidate one-file and multi-hour recording into a single config-driven script.
