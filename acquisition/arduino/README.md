# Arduino

Arduino firmware for generating the shared digital sync pulse train will live here.

The first implementation should start from the existing `simple_acq` firmware, then make the pulse rate and output pins explicit enough for protocol use.
